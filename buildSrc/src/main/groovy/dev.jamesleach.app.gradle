plugins {
    id 'dev.jamesleach.kotlin'
    id 'org.springframework.boot'
}

bootJar {
    layered {
        enabled = true
    }

    // Remove version number from bootJar
    archiveFileName = "${archiveBaseName.get()}.${archiveExtension.get()}"
}