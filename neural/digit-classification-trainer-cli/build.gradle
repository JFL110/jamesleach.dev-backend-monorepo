plugins {
    id 'dev.jamesleach.app'
}

dependencies {
    implementation project(':neural:digit-classification')
    implementation "org.springframework.boot:spring-boot-starter:$springBootVersion"

    testImplementation testFixtures(project(':neural:digit-classification'))
}